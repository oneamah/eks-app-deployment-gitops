name: Publish Frontend Chart To ECR

on:
  workflow_dispatch:
    inputs:
      chart_version:
        description: Chart version to publish (defaults to Chart.yaml version)
        required: false
        type: string
      aws_region:
        description: AWS region for ECR
        required: false
        default: us-east-1
        type: string
  push:
    branches: ["main"]
    paths:
      - "frontend/**"
      - "charts/frontend/**"
      - ".github/workflows/publish-frontend-chart-to-ecr.yml"

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ github.event.inputs.aws_region || 'us-east-1' }}

jobs:
  publish-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Resolve AWS account ID
        id: aws
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          echo "account_id=$ACCOUNT_ID" >> "$GITHUB_OUTPUT"
          echo "registry=$ACCOUNT_ID.dkr.ecr.${AWS_REGION}.amazonaws.com" >> "$GITHUB_OUTPUT"

      - name: Replace account placeholder in frontend chart values
        run: |
          sed -i "s/123456789012/${{ steps.aws.outputs.account_id }}/g" charts/frontend/values.yaml

      - name: Resolve image and chart versions
        id: versions
        shell: bash
        run: |
          IMAGE_TAG="${GITHUB_SHA::12}"
          if [ -n "${{ github.event.inputs.chart_version }}" ]; then
            CHART_VERSION="${{ github.event.inputs.chart_version }}"
          else
            CHART_VERSION="0.1.0-${IMAGE_TAG}"
          fi
          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "chart_version=$CHART_VERSION" >> "$GITHUB_OUTPUT"

      - name: Set frontend chart image tag
        shell: bash
        run: |
          sed -i -E "0,/^  tag: .*/s//  tag: ${{ steps.versions.outputs.image_tag }}/" charts/frontend/values.yaml

      - name: Validate certificate placeholder was configured
        run: |
          if grep -R "certificate/replace-me" charts/frontend/values.yaml; then
            echo "Replace certificate placeholder in charts/frontend/values.yaml before publishing chart."
            exit 1
          fi

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Lint frontend chart
        run: helm lint charts/frontend

      - name: Package frontend chart
        run: |
          mkdir -p dist
          helm package charts/frontend \
            --version "${{ steps.versions.outputs.chart_version }}" \
            --app-version "${{ steps.versions.outputs.image_tag }}" \
            --destination dist

      - name: Ensure frontend OCI repository exists
        run: |
          aws ecr describe-repositories --repository-names frontend-chart/frontend --region "$AWS_REGION" >/dev/null 2>&1 || \
          aws ecr create-repository --repository-name frontend-chart/frontend --region "$AWS_REGION"

      - name: Push frontend chart to ECR OCI
        run: helm push dist/frontend-*.tgz oci://${{ steps.aws.outputs.registry }}/frontend-chart

      - name: Show pushed frontend chart tags
        run: |
          aws ecr describe-images --repository-name frontend-chart/frontend --region "$AWS_REGION" --query 'imageDetails[].imageTags' --output json
